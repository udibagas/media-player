function Header() {
  return (
    <header
      style={{
        padding: "10px",
        backgroundColor: "green",
        textAlign: "center",
        color: "white",
        fontSize: 20,
      }}
    >
      Media Player
    </header>
  );
}

export default Header;
